import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function EventsScreen() {
  return (
    <PlaceholderScreen
      title="Upcoming Events"
      note="Events list, live-events link and stadium event days arrive in M2 — driven entirely from the admin dashboard."
    />
  );
}
