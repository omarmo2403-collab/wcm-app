import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function MoreScreen() {
  return (
    <PlaceholderScreen
      title="More"
      note="About, News, Qibla, Contact, Services, Virtual Tour, Notification settings and Stadium event days arrive in M2–M3."
    />
  );
}
